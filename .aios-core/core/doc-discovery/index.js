/**
 * Doc Discovery Module Entry
 *
 * @module core/doc-discovery
 */

const { DocSearcher } = require('./doc-searcher');
const { ContextHelper } = require('./context-helper');

module.exports = {
  DocSearcher,
  ContextHelper
};
